import React from 'react';

/* This is NOT the Pagination component from Semantic React.
It is not customizable enough, so this is
a custom component using Semantic classes.
*/

const Pagination = () => {

};

export default Pagination;